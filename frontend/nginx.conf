server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 👇 this line is the key that fixes your 404 on refresh
    location / {
        try_files $uri /index.html;
    }
}
