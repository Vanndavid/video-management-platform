services:
  app:
    build:
      context: ./backend
      dockerfile: DockerFile.dev
    container_name: video_app
    volumes:
      - ./backend:/var/www/html
      - storage_dev:/var/www/html/storage
    networks: [web]
    depends_on: [redis]
    command: php-fpm
    expose: ["9000"]

  worker:
    build:
      context: ./backend
      dockerfile: DockerFile.dev
    container_name: video_worker
    volumes:
      - ./backend:/var/www/html
      - storage_dev:/var/www/html/storage
    networks: [web]
    depends_on: [redis]
    command: sh -lc "php artisan queue:work --queue=transcode,default --sleep=1 --tries=1"

  nginx:
    image: nginx:alpine
    container_name: video_nginx
    volumes:
      - ./backend:/var/www/html
      - ./nginx/dev.api.conf:/etc/nginx/conf.d/default.conf:ro
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.videoApi.rule=Host(`video.localhost`) && PathPrefix(`/api`)"
      - "traefik.http.routers.videoApi.entrypoints=web"
      - "traefik.http.services.videoApi.loadbalancer.server.port=80"
    networks: [web]
    depends_on: [app]
    expose: ["80"]

  frontend:
    image: node:22-alpine
    container_name: video_frontend
    working_dir: /app
    volumes:
      - ./frontend:/app
    environment:
      - VITE_API_BASE=/api
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.video.rule=Host(`video.localhost`) && PathPrefix(`/`)"
      - "traefik.http.routers.video.entrypoints=web"
      - "traefik.http.routers.video.priority=1"
      - "traefik.http.services.video.loadbalancer.server.port=5173"
    ports:
      - "5173:5173"
    command: sh -c "npm install && npm run dev -- --host"
    networks: [web]

  redis:
    image: redis:7-alpine
    container_name: video_redis
    ports: ["6379:6379"]
    networks: [web]

networks:
  web:
    external: true

volumes:
  storage_dev:
